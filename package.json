{
  "name": "game-sanguo",
  "private": true,
  "workspaces": ["client", "server"],
  "scripts": {
    "dev": "kill-port 8787 && concurrently -n server,client -c blue,green \"npm run dev -w server\" \"npm run dev -w client\"",
    "build": "npm run build -w client",
    "start": "npm run start -w server",
    "preview": "npm run build -w client && npm run start -w server"
  },
  "devDependencies": {
    "concurrently": "^9.1.2",
    "kill-port": "^2.0.1"
  }
}
